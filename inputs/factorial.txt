main
var input, x;
function factIter(n);
var i, f, x;
{
    let x <- 4;
	let i <- 1;
	let f <- 1;
	while i <= n do
		let f <- f * i;
		let i <- i + 1
	od;
	return f
};
function factRec(n);
{
	if n <= 1 then
		return 1
	fi;
	return call factRec(n - 1) * n
};

{
	let input <- call InputNum();
	#call OutputNum(call factIter(input));
	#call OutputNewLine();
	let x <- call factRec(input);
	call OutputNum(x);
	call OutputNewLine()
}
.